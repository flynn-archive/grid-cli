
```
grid scale digitalocean=3		# scale nodes across supported cloud providers
grid info						# shows grid info, such as hosts
grid start						# host startup command (wraps docker commands)
grid jobs						# list jobs created by grid utility
grid schedule					# schedule a job
grid target						# target a grid cluster
```